{
  "name": "shuiyi",
  "version": "1.0.0",
  "description": "Free bilingual tax guidance for international students",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && npx http-server dist -p 3000 -c-1",
    "serve": "npx http-server dist -p 3000 -c-1",
    "test": "vitest run"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.3"
  }
}
